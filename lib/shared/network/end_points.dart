const login = 'login' ;

const home = 'home';

const categories  =  'categories' ;

const favorites = 'favorites';

const profile = 'profile';

const updateProfile = 'update-profile';

const faqs = 'faqs';

const addresses = 'addresses' ;

const updateAndDeleteAddresses = 'addresses/' ;

const register = 'register';

const termsAndAboutApp1 = 'settings';

const contactApiBuilder = 'contacts';

const search = 'products/search';

const cart = 'carts' ;

const orders = 'orders' ;
